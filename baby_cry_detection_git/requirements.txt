# Baby Cry Detection Model Requirements
# Core machine learning and deep learning libraries
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0

# Audio processing libraries
librosa>=0.10.0
soundfile>=0.12.0

# Scientific computing and data manipulation
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Machine learning utilities
scikit-learn>=1.3.0

# Visualization libraries
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress bars and utilities
tqdm>=4.65.0

# System monitoring
psutil>=5.9.0

# Data validation and type checking (optional but recommended)
pydantic>=2.0.0

# Jupyter notebook support (optional)
jupyter>=1.0.0
ipywidgets>=8.0.0

# Development and testing (optional)
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0

# Audio format support extensions
pydub>=0.25.0  # For additional audio format support
ffmpeg-python>=0.2.0  # For video/audio processing

# Model optimization for deployment
onnx>=1.14.0  # For ONNX export
onnxruntime>=1.15.0  # For ONNX inference

# Configuration management
pyyaml>=6.0.0
configargparse>=1.7.0

# Logging and monitoring
tensorboard>=2.13.0  # For training visualization
wandb>=0.15.0  # For experiment tracking (optional)

# Memory profiling (development)
memory-profiler>=0.61.0

# Additional utilities
pathlib2>=2.3.0  # For Python < 3.4 compatibility
colorlog>=6.7.0  # For colored logging output
requests>=2.31.0  # For HTTP requests (Freesound API)
huggingface-hub>=0.17.0  # For Hugging Face dataset downloads (ICSD)

# Raspberry Pi real-time audio processing
pyaudio>=0.2.11  # For real-time audio streaming on Pi
RPi.GPIO>=0.7.1  # For GPIO control (Pi-specific)
gpiozero>=1.6.2  # Simplified GPIO interface